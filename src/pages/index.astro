---
import { IFrontmatter, IPost } from "@typings";
import Index from "../views/Index";

const allRawPosts = await Astro.glob<IFrontmatter>("../pages/posts/*.md");
---

<Index allPosts={allRawPosts} />
